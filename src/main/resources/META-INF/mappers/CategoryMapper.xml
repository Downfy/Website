<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.downfy.persistence.repositories.category.CategoryRepository">

    <select id="findAll" resultType="CategoryDomain">
        SELECT PARENT, NAME, URL, ENABLED, SORT 
        FROM categories
        ORDER BY PARENT, URL
    </select>
    
    <select id="findByUrl" resultType="CategoryDomain">
        SELECT PARENT, NAME, URL, ENABLED, SORT 
        FROM categories
        WHERE URL = #{url}
    </select>
    
    <insert id="save" parameterType="CategoryDomain">
        INSERT INTO categories
        (NAME, URL, PARENT)
        VALUES
        (#{name},#{url},#{parent})
    </insert>
    
    <update id="update">
        UPDATE categories
        SET 
        PARENT = #{parent},
        NAME = #{name},
        URL = #{url},
        SORT = #{sort},
        ENABLED = #{enabled}
        WHERE URL = #{url}
    </update>
    
    <delete id="delete">
        DELETE FROM categories
        WHERE URL = #{url}
    </delete>
</mapper>